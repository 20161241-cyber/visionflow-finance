# Dependencias de VisionFlow Finance
# Instalar con: pip install -r requirements.txt

# ─── Framework UI Multiplataforma ────────────────────────────────────────────
flet>=0.24.0                    # UI multiplataforma (Mobile + Desktop)

# ─── Procesamiento de Imágenes ───────────────────────────────────────────────
Pillow>=10.0.0                  # Manipulación de imágenes (Soporte Móvil)

# ─── OCR (Solo Desktop — no disponible en Android) ──────────────────────────
# pytesseract>=0.3.10           # Descomenta si usas la app solo en Desktop
# supabase>=2.4.0               # Descomenta si configuras Supabase

# ─── Utilidades ───────────────────────────────────────────────────────────────
# python-dotenv>=1.0.0          # Descomenta si usas variables de entorno (.env)
